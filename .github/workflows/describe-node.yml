# Generated by ocx-mirror v0.5.0 (rev unknown)
# DO NOT EDIT — re-run `ocx-mirror package pipeline generate ci` to update.
# Source: node/mirror.yml

name: describe-node

on:
  workflow_dispatch:
  push:
    branches: [main]
    paths:
      # `script:` paths resolve from the repository root, not from node/ —
      # keep this spec's scripts under node/ so editing one triggers this run.
      - node/**
      - mirror-base.yml
      - .github/workflows/describe-node.yml

concurrency:
  group: mirror-${{ github.workflow }}-describe
  cancel-in-progress: false

jobs:
  describe:
    runs-on: ubuntu-latest
    permissions:
      contents: read
      packages: write
    steps:
      - uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd  # v6.0.2
      - uses: ocx-sh/setup-ocx@de8e3366f812941423985eaccae28663ef192e8b  # v1.3.0
      # ghcr.io authenticates with this run's own GITHUB_TOKEN, which is
      # always present — so the credential probe is constant. The shared
      # OCX_MIRROR_REGISTRY_* org secrets hold ocx.sh credentials and are
      # deliberately not read here.
      - name: Detect registry credentials
        id: creds
        run: echo "have=true" >> "${GITHUB_OUTPUT}"
      # docker login so ocx picks the credential up via its native-credential
      # fallback (`get_docker_auth` in crates/ocx_lib/src/auth.rs).
      - name: Login to ghcr.io
        run: |
          echo "${{ secrets.GITHUB_TOKEN }}" \
            | docker login ghcr.io \
                -u "${{ github.actor }}" \
                --password-stdin
      - name: Publish catalog metadata
        if: ${{ steps.creds.outputs.have == 'true' }}
        run: |
          ocx-mirror package pipeline describe --spec node/mirror.yml
